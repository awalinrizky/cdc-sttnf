import { f as createComponent, k as renderComponent, r as renderTemplate, m as maybeRenderHead } from '../../chunks/astro/server_s_qG7lfK.mjs';
import 'piccolore';
import { $ as $$BaseLayout } from '../../chunks/BaseLayout_CTuTJhWc.mjs';
/* empty css                                    */
export { renderers } from '../../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Event = createComponent(async ($$result, $$props, $$slots) => {
  return renderTemplate`${renderComponent($$result, "BaseLayout", $$BaseLayout, { "title": "CDC STT-NF | Kelola Lowongan", "data-astro-cid-airzqifm": true }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template([" ", '<section class="admin-job-page" data-astro-cid-airzqifm> <div class="container" data-astro-cid-airzqifm> <div class="page-header" data-astro-cid-airzqifm> <div data-astro-cid-airzqifm> <p class="eyebrow" data-astro-cid-airzqifm>Admin Panel</p> <h1 data-astro-cid-airzqifm>Kelola Lowongan</h1> <p class="subtext" data-astro-cid-airzqifm>\nTambah, edit, hapus, dan atur status lowongan kerja.\n</p> </div> <div class="page-actions" data-astro-cid-airzqifm> <a href="/admin" class="btn btn-outline" data-astro-cid-airzqifm>Kembali ke Dashboard</a> <button id="logoutBtn" class="btn btn-danger" data-astro-cid-airzqifm>Logout</button> </div> </div> <div class="layout-grid" data-astro-cid-airzqifm> <div class="form-card" data-astro-cid-airzqifm> <h2 id="formTitle" data-astro-cid-airzqifm>Tambah Lowongan</h2> <form id="jobForm" class="job-form" data-astro-cid-airzqifm> <div class="form-group" data-astro-cid-airzqifm> <label for="title" data-astro-cid-airzqifm>Judul Lowongan</label> <input type="text" id="title" required data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="companyName" data-astro-cid-airzqifm>Nama Perusahaan</label> <input type="text" id="companyName" data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="jobLocation" data-astro-cid-airzqifm>Lokasi</label> <input type="text" id="jobLocation" required data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="applyUrl" data-astro-cid-airzqifm>Link Apply</label> <input type="url" id="applyUrl" placeholder="https://..." data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="description" data-astro-cid-airzqifm>Deskripsi</label> <textarea id="description" rows="6" data-astro-cid-airzqifm></textarea> </div> <div class="checkbox-group" data-astro-cid-airzqifm> <input type="checkbox" id="isActive" checked data-astro-cid-airzqifm> <label for="isActive" data-astro-cid-airzqifm>Tampilkan di publik</label> </div> <div class="form-actions" data-astro-cid-airzqifm> <button type="submit" id="submitBtn" class="btn btn-primary" data-astro-cid-airzqifm>\nSimpan Lowongan\n</button> <button type="button" id="cancelEditBtn" class="btn btn-outline" hidden data-astro-cid-airzqifm>\nBatal Edit\n</button> </div> <p id="formMessage" class="message" data-astro-cid-airzqifm></p> </form> </div> <div class="list-card" data-astro-cid-airzqifm> <div class="list-head" data-astro-cid-airzqifm> <h2 data-astro-cid-airzqifm>Daftar Lowongan</h2> <input type="text" id="searchInput" placeholder="Cari judul / perusahaan / lokasi..." data-astro-cid-airzqifm> </div> <div id="jobList" class="job-list" data-astro-cid-airzqifm> <p class="empty" data-astro-cid-airzqifm>Memuat data lowongan...</p> </div> </div> </div> </div> </section>  <script type="module">\n    import { supabase } from "../../lib/supabase.js";\n\n    const jobForm = document.getElementById("jobForm");\n    const formTitle = document.getElementById("formTitle");\n    const submitBtn = document.getElementById("submitBtn");\n    const cancelEditBtn = document.getElementById("cancelEditBtn");\n    const formMessage = document.getElementById("formMessage");\n    const jobList = document.getElementById("jobList");\n    const searchInput = document.getElementById("searchInput");\n    const logoutBtn = document.getElementById("logoutBtn");\n\n    const titleInput = document.getElementById("title");\n    const companyNameInput = document.getElementById("companyName");\n    const jobLocationInput = document.getElementById("jobLocation");\n    const applyUrlInput = document.getElementById("applyUrl");\n    const descriptionInput = document.getElementById("description");\n    const isActiveInput = document.getElementById("isActive");\n\n    let editingId = null;\n    let events = [];\n\n    function setMessage(text, type = "error") {\n      formMessage.textContent = text;\n      formMessage.className = `message ${type === "success" ? "success" : ""}`;\n    }\n\n    function resetForm() {\n      editingId = null;\n      formTitle.textContent = "Tambah Lowongan";\n      submitBtn.textContent = "Simpan Lowongan";\n      cancelEditBtn.hidden = true;\n      jobForm.reset();\n      isActiveInput.checked = true;\n      setMessage("");\n    }\n\n    function fillForm(job) {\n      editingId = job.id;\n      formTitle.textContent = "Edit Lowongan";\n      submitBtn.textContent = "Update Lowongan";\n      cancelEditBtn.hidden = false;\n\n      titleInput.value = job.title || "";\n      companyNameInput.value = job.company_name || "";\n      jobLocationInput.value = job.location || "";\n      applyUrlInput.value = job.apply_url || "";\n      descriptionInput.value = job.description || "";\n      isActiveInput.checked = !!job.is_active;\n\n      window.scrollTo({ top: 0, behavior: "smooth" });\n    }\n\n    function renderevents(data) {\n      jobList.innerHTML = "";\n\n      if (!data.length) {\n        jobList.innerHTML = `<p class="empty">Belum ada lowongan.</p>`;\n        return;\n      }\n\n      data.forEach((job) => {\n        const item = document.createElement("div");\n        item.className = "job-item";\n\n        const top = document.createElement("div");\n        top.className = "job-top";\n\n        const info = document.createElement("div");\n\n        const title = document.createElement("div");\n        title.className = "job-title";\n        title.textContent = job.title;\n\n        const meta = document.createElement("div");\n        meta.className = "job-meta";\n        meta.textContent = `${job.company_name || "Perusahaan belum diisi"} \u2022 ${job.location}`;\n\n        info.appendChild(title);\n        info.appendChild(meta);\n\n        const badge = document.createElement("span");\n        badge.className = `status-badge ${job.is_active ? "status-active" : "status-inactive"}`;\n        badge.textContent = job.is_active ? "Aktif" : "Nonaktif";\n\n        top.appendChild(info);\n        top.appendChild(badge);\n\n        const desc = document.createElement("div");\n        desc.className = "job-description";\n        desc.textContent = job.description || "Belum ada deskripsi.";\n\n        const actions = document.createElement("div");\n        actions.className = "job-actions";\n\n        const editBtn = document.createElement("button");\n        editBtn.className = "btn btn-outline";\n        editBtn.textContent = "Edit";\n        editBtn.addEventListener("click", () => fillForm(job));\n\n        const deleteBtn = document.createElement("button");\n        deleteBtn.className = "btn btn-danger";\n        deleteBtn.textContent = "Hapus";\n        deleteBtn.addEventListener("click", () => deleteJob(job.id));\n\n        actions.appendChild(editBtn);\n        actions.appendChild(deleteBtn);\n\n        item.appendChild(top);\n        item.appendChild(desc);\n        item.appendChild(actions);\n\n        jobList.appendChild(item);\n      });\n    }\n\n    async function guardAdmin() {\n      const {\n        data: { session },\n      } = await supabase.auth.getSession();\n\n      if (!session) {\n        window.location.href = "/login";\n        return false;\n      }\n\n      const { data: profile, error } = await supabase\n        .from("profiles")\n        .select("role")\n        .eq("id", session.user.id)\n        .single();\n\n      if (error || !profile) {\n        await supabase.auth.signOut();\n        window.location.href = "/login";\n        return false;\n      }\n\n      if (profile.role !== "admin" && profile.role !== "superadmin") {\n        window.location.href = "/403";\n        return false;\n      }\n\n      return true;\n    }\n\n    async function loadevents() {\n      jobList.innerHTML = `<p class="empty">Memuat data lowongan...</p>`;\n\n      const { data, error } = await supabase\n        .from("events")\n        .select("*")\n        .order("created_at", { ascending: false });\n\n      if (error) {\n        jobList.innerHTML = `<p class="empty">Gagal memuat lowongan.</p>`;\n        return;\n      }\n\n      events = data || [];\n      renderevents(events);\n    }\n\n    async function createJob(payload) {\n      const { error } = await supabase.from("events").insert(payload);\n      return error;\n    }\n\n    async function updateJob(id, payload) {\n      const { error } = await supabase\n        .from("events")\n        .update(payload)\n        .eq("id", id);\n      return error;\n    }\n\n    async function deleteJob(id) {\n      const confirmed = confirm("Yakin ingin menghapus lowongan ini?");\n      if (!confirmed) return;\n\n      const { error } = await supabase.from("events").delete().eq("id", id);\n\n      if (error) {\n        alert("Gagal menghapus lowongan: " + error.message);\n        return;\n      }\n\n      if (editingId === id) resetForm();\n      await loadevents();\n    }\n\n    function getPayload() {\n      return {\n        title: titleInput.value.trim(),\n        company_name: companyNameInput.value.trim(),\n        location: jobLocationInput.value.trim(),\n        apply_url: applyUrlInput.value.trim(),\n        description: descriptionInput.value.trim(),\n        is_active: isActiveInput.checked,\n      };\n    }\n\n    jobForm.addEventListener("submit", async (e) => {\n      e.preventDefault();\n\n      const payload = getPayload();\n\n      if (!payload.title || !payload.location) {\n        setMessage("Judul lowongan dan lokasi wajib diisi.");\n        return;\n      }\n\n      submitBtn.disabled = true;\n      submitBtn.textContent = editingId ? "Mengupdate..." : "Menyimpan...";\n      setMessage("");\n\n      let result;\n\n      if (editingId) {\n        result = await updateJob(editingId, payload);\n      } else {\n        result = await createJob(payload);\n      }\n\n      const { error } = result;\n\n      if (error) {\n        console.error("Save job error:", error);\n        setMessage("Gagal menyimpan lowongan. Silakan coba lagi.");\n        submitBtn.disabled = false;\n        submitBtn.textContent = editingId\n          ? "Update Lowongan"\n          : "Simpan Lowongan";\n        return;\n      }\n\n      setMessage(\n        editingId\n          ? "Lowongan berhasil diperbarui."\n          : "Lowongan berhasil ditambahkan.",\n        "success",\n      );\n\n      resetForm();\n      submitBtn.disabled = false;\n      submitBtn.textContent = "Simpan Lowongan";\n      await loadJobs();\n    });\n\n    cancelEditBtn.addEventListener("click", resetForm);\n\n    searchInput.addEventListener("input", () => {\n      const keyword = searchInput.value.toLowerCase().trim();\n\n      if (!keyword) {\n        renderevents(events);\n        return;\n      }\n\n      const filtered = events.filter((job) => {\n        return (\n          (job.title || "").toLowerCase().includes(keyword) ||\n          (job.company_name || "").toLowerCase().includes(keyword) ||\n          (job.location || "").toLowerCase().includes(keyword)\n        );\n      });\n\n      renderevents(filtered);\n    });\n\n    logoutBtn.addEventListener("click", async () => {\n      const { error } = await supabase.auth.signOut();\n\n      if (!error) {\n        window.location.href = "/login";\n      }\n    });\n\n    const allowed = await guardAdmin();\n    if (allowed) {\n      resetForm();\n      await loadevents();\n    }\n  <\/script> '], [" ", '<section class="admin-job-page" data-astro-cid-airzqifm> <div class="container" data-astro-cid-airzqifm> <div class="page-header" data-astro-cid-airzqifm> <div data-astro-cid-airzqifm> <p class="eyebrow" data-astro-cid-airzqifm>Admin Panel</p> <h1 data-astro-cid-airzqifm>Kelola Lowongan</h1> <p class="subtext" data-astro-cid-airzqifm>\nTambah, edit, hapus, dan atur status lowongan kerja.\n</p> </div> <div class="page-actions" data-astro-cid-airzqifm> <a href="/admin" class="btn btn-outline" data-astro-cid-airzqifm>Kembali ke Dashboard</a> <button id="logoutBtn" class="btn btn-danger" data-astro-cid-airzqifm>Logout</button> </div> </div> <div class="layout-grid" data-astro-cid-airzqifm> <div class="form-card" data-astro-cid-airzqifm> <h2 id="formTitle" data-astro-cid-airzqifm>Tambah Lowongan</h2> <form id="jobForm" class="job-form" data-astro-cid-airzqifm> <div class="form-group" data-astro-cid-airzqifm> <label for="title" data-astro-cid-airzqifm>Judul Lowongan</label> <input type="text" id="title" required data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="companyName" data-astro-cid-airzqifm>Nama Perusahaan</label> <input type="text" id="companyName" data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="jobLocation" data-astro-cid-airzqifm>Lokasi</label> <input type="text" id="jobLocation" required data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="applyUrl" data-astro-cid-airzqifm>Link Apply</label> <input type="url" id="applyUrl" placeholder="https://..." data-astro-cid-airzqifm> </div> <div class="form-group" data-astro-cid-airzqifm> <label for="description" data-astro-cid-airzqifm>Deskripsi</label> <textarea id="description" rows="6" data-astro-cid-airzqifm></textarea> </div> <div class="checkbox-group" data-astro-cid-airzqifm> <input type="checkbox" id="isActive" checked data-astro-cid-airzqifm> <label for="isActive" data-astro-cid-airzqifm>Tampilkan di publik</label> </div> <div class="form-actions" data-astro-cid-airzqifm> <button type="submit" id="submitBtn" class="btn btn-primary" data-astro-cid-airzqifm>\nSimpan Lowongan\n</button> <button type="button" id="cancelEditBtn" class="btn btn-outline" hidden data-astro-cid-airzqifm>\nBatal Edit\n</button> </div> <p id="formMessage" class="message" data-astro-cid-airzqifm></p> </form> </div> <div class="list-card" data-astro-cid-airzqifm> <div class="list-head" data-astro-cid-airzqifm> <h2 data-astro-cid-airzqifm>Daftar Lowongan</h2> <input type="text" id="searchInput" placeholder="Cari judul / perusahaan / lokasi..." data-astro-cid-airzqifm> </div> <div id="jobList" class="job-list" data-astro-cid-airzqifm> <p class="empty" data-astro-cid-airzqifm>Memuat data lowongan...</p> </div> </div> </div> </div> </section>  <script type="module">\n    import { supabase } from "../../lib/supabase.js";\n\n    const jobForm = document.getElementById("jobForm");\n    const formTitle = document.getElementById("formTitle");\n    const submitBtn = document.getElementById("submitBtn");\n    const cancelEditBtn = document.getElementById("cancelEditBtn");\n    const formMessage = document.getElementById("formMessage");\n    const jobList = document.getElementById("jobList");\n    const searchInput = document.getElementById("searchInput");\n    const logoutBtn = document.getElementById("logoutBtn");\n\n    const titleInput = document.getElementById("title");\n    const companyNameInput = document.getElementById("companyName");\n    const jobLocationInput = document.getElementById("jobLocation");\n    const applyUrlInput = document.getElementById("applyUrl");\n    const descriptionInput = document.getElementById("description");\n    const isActiveInput = document.getElementById("isActive");\n\n    let editingId = null;\n    let events = [];\n\n    function setMessage(text, type = "error") {\n      formMessage.textContent = text;\n      formMessage.className = \\`message \\${type === "success" ? "success" : ""}\\`;\n    }\n\n    function resetForm() {\n      editingId = null;\n      formTitle.textContent = "Tambah Lowongan";\n      submitBtn.textContent = "Simpan Lowongan";\n      cancelEditBtn.hidden = true;\n      jobForm.reset();\n      isActiveInput.checked = true;\n      setMessage("");\n    }\n\n    function fillForm(job) {\n      editingId = job.id;\n      formTitle.textContent = "Edit Lowongan";\n      submitBtn.textContent = "Update Lowongan";\n      cancelEditBtn.hidden = false;\n\n      titleInput.value = job.title || "";\n      companyNameInput.value = job.company_name || "";\n      jobLocationInput.value = job.location || "";\n      applyUrlInput.value = job.apply_url || "";\n      descriptionInput.value = job.description || "";\n      isActiveInput.checked = !!job.is_active;\n\n      window.scrollTo({ top: 0, behavior: "smooth" });\n    }\n\n    function renderevents(data) {\n      jobList.innerHTML = "";\n\n      if (!data.length) {\n        jobList.innerHTML = \\`<p class="empty">Belum ada lowongan.</p>\\`;\n        return;\n      }\n\n      data.forEach((job) => {\n        const item = document.createElement("div");\n        item.className = "job-item";\n\n        const top = document.createElement("div");\n        top.className = "job-top";\n\n        const info = document.createElement("div");\n\n        const title = document.createElement("div");\n        title.className = "job-title";\n        title.textContent = job.title;\n\n        const meta = document.createElement("div");\n        meta.className = "job-meta";\n        meta.textContent = \\`\\${job.company_name || "Perusahaan belum diisi"} \u2022 \\${job.location}\\`;\n\n        info.appendChild(title);\n        info.appendChild(meta);\n\n        const badge = document.createElement("span");\n        badge.className = \\`status-badge \\${job.is_active ? "status-active" : "status-inactive"}\\`;\n        badge.textContent = job.is_active ? "Aktif" : "Nonaktif";\n\n        top.appendChild(info);\n        top.appendChild(badge);\n\n        const desc = document.createElement("div");\n        desc.className = "job-description";\n        desc.textContent = job.description || "Belum ada deskripsi.";\n\n        const actions = document.createElement("div");\n        actions.className = "job-actions";\n\n        const editBtn = document.createElement("button");\n        editBtn.className = "btn btn-outline";\n        editBtn.textContent = "Edit";\n        editBtn.addEventListener("click", () => fillForm(job));\n\n        const deleteBtn = document.createElement("button");\n        deleteBtn.className = "btn btn-danger";\n        deleteBtn.textContent = "Hapus";\n        deleteBtn.addEventListener("click", () => deleteJob(job.id));\n\n        actions.appendChild(editBtn);\n        actions.appendChild(deleteBtn);\n\n        item.appendChild(top);\n        item.appendChild(desc);\n        item.appendChild(actions);\n\n        jobList.appendChild(item);\n      });\n    }\n\n    async function guardAdmin() {\n      const {\n        data: { session },\n      } = await supabase.auth.getSession();\n\n      if (!session) {\n        window.location.href = "/login";\n        return false;\n      }\n\n      const { data: profile, error } = await supabase\n        .from("profiles")\n        .select("role")\n        .eq("id", session.user.id)\n        .single();\n\n      if (error || !profile) {\n        await supabase.auth.signOut();\n        window.location.href = "/login";\n        return false;\n      }\n\n      if (profile.role !== "admin" && profile.role !== "superadmin") {\n        window.location.href = "/403";\n        return false;\n      }\n\n      return true;\n    }\n\n    async function loadevents() {\n      jobList.innerHTML = \\`<p class="empty">Memuat data lowongan...</p>\\`;\n\n      const { data, error } = await supabase\n        .from("events")\n        .select("*")\n        .order("created_at", { ascending: false });\n\n      if (error) {\n        jobList.innerHTML = \\`<p class="empty">Gagal memuat lowongan.</p>\\`;\n        return;\n      }\n\n      events = data || [];\n      renderevents(events);\n    }\n\n    async function createJob(payload) {\n      const { error } = await supabase.from("events").insert(payload);\n      return error;\n    }\n\n    async function updateJob(id, payload) {\n      const { error } = await supabase\n        .from("events")\n        .update(payload)\n        .eq("id", id);\n      return error;\n    }\n\n    async function deleteJob(id) {\n      const confirmed = confirm("Yakin ingin menghapus lowongan ini?");\n      if (!confirmed) return;\n\n      const { error } = await supabase.from("events").delete().eq("id", id);\n\n      if (error) {\n        alert("Gagal menghapus lowongan: " + error.message);\n        return;\n      }\n\n      if (editingId === id) resetForm();\n      await loadevents();\n    }\n\n    function getPayload() {\n      return {\n        title: titleInput.value.trim(),\n        company_name: companyNameInput.value.trim(),\n        location: jobLocationInput.value.trim(),\n        apply_url: applyUrlInput.value.trim(),\n        description: descriptionInput.value.trim(),\n        is_active: isActiveInput.checked,\n      };\n    }\n\n    jobForm.addEventListener("submit", async (e) => {\n      e.preventDefault();\n\n      const payload = getPayload();\n\n      if (!payload.title || !payload.location) {\n        setMessage("Judul lowongan dan lokasi wajib diisi.");\n        return;\n      }\n\n      submitBtn.disabled = true;\n      submitBtn.textContent = editingId ? "Mengupdate..." : "Menyimpan...";\n      setMessage("");\n\n      let result;\n\n      if (editingId) {\n        result = await updateJob(editingId, payload);\n      } else {\n        result = await createJob(payload);\n      }\n\n      const { error } = result;\n\n      if (error) {\n        console.error("Save job error:", error);\n        setMessage("Gagal menyimpan lowongan. Silakan coba lagi.");\n        submitBtn.disabled = false;\n        submitBtn.textContent = editingId\n          ? "Update Lowongan"\n          : "Simpan Lowongan";\n        return;\n      }\n\n      setMessage(\n        editingId\n          ? "Lowongan berhasil diperbarui."\n          : "Lowongan berhasil ditambahkan.",\n        "success",\n      );\n\n      resetForm();\n      submitBtn.disabled = false;\n      submitBtn.textContent = "Simpan Lowongan";\n      await loadJobs();\n    });\n\n    cancelEditBtn.addEventListener("click", resetForm);\n\n    searchInput.addEventListener("input", () => {\n      const keyword = searchInput.value.toLowerCase().trim();\n\n      if (!keyword) {\n        renderevents(events);\n        return;\n      }\n\n      const filtered = events.filter((job) => {\n        return (\n          (job.title || "").toLowerCase().includes(keyword) ||\n          (job.company_name || "").toLowerCase().includes(keyword) ||\n          (job.location || "").toLowerCase().includes(keyword)\n        );\n      });\n\n      renderevents(filtered);\n    });\n\n    logoutBtn.addEventListener("click", async () => {\n      const { error } = await supabase.auth.signOut();\n\n      if (!error) {\n        window.location.href = "/login";\n      }\n    });\n\n    const allowed = await guardAdmin();\n    if (allowed) {\n      resetForm();\n      await loadevents();\n    }\n  <\/script> '])), maybeRenderHead()) })}`;
}, "C:/astro-project/src/pages/admin/event.astro", void 0);

const $$file = "C:/astro-project/src/pages/admin/event.astro";
const $$url = "/admin/event";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Event,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
